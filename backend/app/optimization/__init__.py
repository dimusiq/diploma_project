"""Детерминированный слоттинг поверх существующих Item, DES и pending-actions.

LLM ячейки не выбирает: оценки считает этот пакет, мутация после одобрения —
существующий ``create_transfer_task``.
"""
